/**
 * set_string : function that sets the value of a pointer to a cha
 * @y: point to an address of string y
 * @to: the string set the value
 *
 * Return: nothing
 */
void set_string(char **y, char *to)
{
	*y = to;
}
